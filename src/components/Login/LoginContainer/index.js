import React from "react";
import { View, Text } from "react-native";

export default LoginContainer = ({ containerStyle }) => {
  return (
    <View style={containerStyle}>
      <Text style={{ color: "black" }}>Here is the container</Text>
    </View>
  );
};
